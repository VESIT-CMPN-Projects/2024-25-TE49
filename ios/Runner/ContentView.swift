import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Welcome to Journey Gennie!")
                .font(.largeTitle)
                .padding()
            // Add other UI elements here
        }
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
